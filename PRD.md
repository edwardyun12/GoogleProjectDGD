# 네트워킹 파티 앱 — MVP 기획서

목적: MVP 구현을 위한 기능 정의
제외: 디자인, 시안, 스타일 가이드

---

## 1. 무엇을 푸는가

네트워킹 파티에서 "이제 네트워킹 시간입니다"라는 안내는 아무 행동 지침이 되지 못한다.
외향적인 참석자는 알아서 움직이지만, **먼저 말을 걸 명분이 없는 사람은 그 자리에 멈춰 선다.**

이 앱은 명분을 만들어 주는 대신 **할 일을 계속 던져 준다.**
"말을 거세요"가 아니라 "지금 이걸 하세요"로 바꾸는 것이 설계의 축이다.

**핵심 사용자** — 네트워킹 파티 참석자 중 먼저 다가가기 어려워하는 사람
**부차 사용자** — 파티를 여는 호스트

---

## 2. 두 개의 트랙

**트랙 A — 인맥 카드 수집 (상시)**
QR을 보여주고 상대가 스캔하면 카드가 쌓인다.
QR을 내미는 행위 자체가 말을 거는 명분이 된다. **이것이 MVP의 최소 기능이다.**

**트랙 B — 미션 (시간표대로 자동 발행)**
파티 시작과 함께 미션 시간표가 돌아간다. 호스트는 중간에 깜짝 미션을 끼워 넣을 수 있다.
시상은 파티 종료 후 누적 결과로 한 번에 한다.

---

## 3. 전체 흐름

```
[호스트] 파티 생성 · 미션 시간표 작성
      ↓
[참가자] 파티 QR 스캔 → /enter
      ↓
      로그인 또는 회원가입 → /profile 작성
      ↓
[호스트] 파티 시작
      ↓
 ┌──────── 파티 진행 중 ────────┐
 │ 시간표대로 미션 자동 발행      │
 │ 호스트가 깜짝 미션 삽입 가능    │
 │ QR 교환으로 인맥 카드 수집     │
 │ 미션 성공·실패 누적           │
 └─────────────────────────────┘
      ↓
[호스트] 파티 종료 → 누적 결과로 시상
```

---

## 4. 참가자 화면

### `/enter` — 입장

- 중앙: QR 스캔 창
- 하단 중앙: `호스트 설정 문구` / 개행 / `'파티명'으로 초대합니다`
- 파티 QR 스캔 성공 → 로그인 모달

### `/login_modal` — 로그인 / 가입

- 이름(닉네임) + 비밀번호로 로그인
- 신규면 가입 → `/profile`로 이동해 프로필 작성
- 비밀번호는 **가입 시점에만** 입력받는다 (프로필 수정 화면에 노출하지 않는다)

### `/profile` — 프로필

항목:
- 이름(닉네임)
- 나이
- 성별
- MBTI
- 인상착의

인상착의는 파티장에서 서로를 찾고, "혹시 파란 셔츠 입으신 분?"처럼 말을 걸 근거가 되므로 필수로 둔다.

작성 완료 후에도 수정할 수 있다. 비밀번호 변경은 별도 경로로 분리한다.

### `/home` — 홈

- 상단 배너: `호스트 설정 문구` / 개행 / `지금 N명이 파티에 참여해 있어요`
- **현재 진행 중인 미션을 상시 노출한다.** 미션 모달을 닫았거나 놓친 사람도 지금 뭘 해야 하는지 알 수 있어야 한다
- 내 프로필 내용 표시 및 수정 진입

### `/my_qr` — 내 QR 보여주기

**MVP에서 빠지면 안 되는 화면이다.** 양쪽 모두 스캔만 하려 하면 교환이 성립하지 않는다.

- 내 프로필로 생성된 QR을 전체 화면으로 표시
- "제 프로필 추가하세요" 같은 안내 문구를 함께 노출한다 — 첫마디를 대신하는 역할
- 홈과 `/card`에서 한 번의 조작으로 도달한다. **스캔보다 접근성이 높아야 한다**

### `/card` — 인맥 카드

- 카드 리스트 (스캔 성공 시 즉시 추가)
- 하단 바: `카드 추가하기` → QR 스캔 창
- 하단 바: `내 QR 보여주기` → `/my_qr`
- 리스트가 비어 있을 때(0명)의 상태를 별도로 정의한다. 이 앱은 첫 교환이 가장 어렵다

### `/mission_modal` — 미션

- 상단 중앙: 미션 문구
- 남은 시간 표시
- 하단: 성공 / 실패 버튼
- 성공·실패는 프로필별로 카운팅

---

## 5. 호스트 화면

### 파티 생성

- 파티명
- 호스트 설정 문구 (`/enter`와 `/home` 배너에 노출)
- 파티 QR 발급

### 미션 시간표 작성

미션 하나마다:
- 미션 내용
- 제한 시간 (미션마다 다르게 설정 가능)
- 순서

리스트 전체:
- 순서와 소요 시간이 보이는 타임라인
- 전체 합계 시간 — 파티 예정 시간을 넘기면 경고
- 추가 · 삭제 · 순서 변경

### 파티 대시보드 (진행 중)

- 참가자 수, 총 교환 횟수, 현재 미션과 남은 시간
- **깜짝 미션 발행** — 내용과 제한 시간만 정하면 두세 번의 조작으로 즉시 발행
- 깜짝 미션 발행 시 이후 시간표가 어떻게 되는지 표시

### 시상

- 누적 결과 확인 (인맥 수, 미션 성공 횟수)
- 베네핏 지급

---

## 6. 데이터

| 엔티티 | 내용 |
|---|---|
| 파티 | 파티명, 호스트 설정 문구, QR, 상태(준비/진행/종료) |
| 참가자 | 닉네임, 비밀번호, 나이, 성별, MBTI, 인상착의 |
| 미션 | 내용, 제한 시간, 순서, 종류(예정/깜짝) |
| 미션 결과 | 참가자별 성공·실패 카운트 |
| 인맥 카드 | 스캔한 사람, 스캔당한 사람, 시각 |

---

## 7. 미션 판정에 대한 주의

현재 `/mission_modal`은 참가자가 성공·실패를 직접 누르는 구조다.
시상과 연결되면 모두가 성공을 누르게 되므로, MVP에서 아래 중 하나를 정한다.

- **자동 판정** — "N명과 인맥 맺기" 같은 미션은 QR 교환 기록으로 판정한다
- **자기 신고 유지** — 대신 시상 기준에서 제외하거나 가중치를 낮춘다

MVP에서는 자동 판정이 가능한 미션을 우선 설계하는 편이 안전하다.

---

## 8. 아직 정하지 않은 것

- 미션의 종류와 형식
- 미션과 미션 사이에 자유 시간을 둘지, 연달아 붙일지
- 깜짝 미션이 진행 중인 미션을 대체하는지, 동시에 도는지
- 파티 종료 후 인맥 카드의 지속 여부
- 휴대폰 맞대기 방식 (QR 대안으로 보류)